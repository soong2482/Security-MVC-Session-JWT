<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.SecurityMVC.LoginInfo.Mapper.UserMapper">

    <select id="FindById" resultType="java.lang.String">
        SELECT username FROM users WHERE username = #{username}
    </select>

    <select id="FindByPassword" resultType="java.lang.String">
        SELECT password FROM users WHERE username = #{username}
    </select>

    <select id="FindByEnabled" resultType="java.lang.Boolean">
        SELECT enabled FROM users WHERE username = #{username}
    </select>
    <select id="FindByUserDetail" resultType="com.spring.SecurityMVC.LoginInfo.Domain.User">
        SELECT * FROM users LEFT OUTER JOIN authorities ON users.username = #{username} AND authorities.username = #{username} WHERE users.username= #{username};
    </select>
</mapper>
